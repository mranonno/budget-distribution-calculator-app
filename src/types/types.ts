// Teams type
export type Team = {
  id: number;
  name: string;
  percentage: string;
  amount: number;
  deleteOption: boolean;
};

// Founder type
export type Founder = {
  name: string;
  amount: number;
  color: string;
};
